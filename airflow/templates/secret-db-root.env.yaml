apiVersion: v1
kind: Secret
metadata:
  name: airflow-db-root
type: Opaque
data:
  DB_CONNECTION_POSTGRESQL: {{ tuple "oslo_db_postgresql" "internal" "admin" "postgresql" . | include "helm-toolkit.authenticated_endpoint_uri_lookup" | b64enc }}
